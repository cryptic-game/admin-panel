import { SlideoutOutletDirective } from './slideout-outlet.directive';

describe('SlideoutOutletDirective', () => {
  it('should create an instance', () => {
    const directive = new SlideoutOutletDirective();
    expect(directive).toBeTruthy();
  });
});
